.container#service

:coffee
  init = ->
    NProgress.start()
    Tabletop.init
      key: spreadsheet_key
      callback: showInfo
      simpleSheet: false
    return

  showInfo = (data, tabletop) ->
    html = Handlebars.templates['makeup'](data['Makeup'].elements)
    $("#service").append(html);
    NProgress.done()
    return processed_data

  $ ->
    init()

  spreadsheet_key = '1j0LdzGrtjKGadzzDS75Jiu7exAAMbv1OHQfLjpBslvs'
